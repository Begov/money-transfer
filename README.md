# Платёжная система на Go с поддержкой потокобезопасности

Этот проект реализует простую платежную систему на языке Go с поддержкой многопоточности. Основная цель — моделирование перевода средств между пользователями с безопасной обработкой балансов в конкурентной среде.

---

## 🧩 Версии проекта

### v1 — Основы
- Создание пользователей и управление балансом
- Методы `Deposit` и `Withdraw`
- Проверка баланса перед снятием средств

### v2 — Транзакции
- Введение структур `Transaction` и `PaymentSystem`
- Обработка переводов между пользователями
- Хранение списка пользователей и транзакций

### v3 — Параллельная обработка
- Горрутины и каналы для одновременной обработки транзакций
- 3 воркера для выполнения переводов
- Возможны гонки данных при одновременном доступе к балансам

### v4 — Потокобезопасность
- Добавлен `sync.Mutex` для защиты данных пользователя
- Методы `Deposit` и `Withdraw` стали потокобезопасными
- Транзакции обрабатываются параллельно без гонок

---

## ▶️ Запуск проекта

```bash
git clone https://github.com/Begov/money-transfer.git
cd money-transfer
go run main.go
